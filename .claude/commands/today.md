Regenerate `docs/WORK_INSTRUCTIONS.md`.

1. Read `CLAUDE.md`, `docs/ROADMAP.md`, `docs/WORK_INSTRUCTIONS.md`.
2. Ask Justin what he finished. Wait.
3. Move finished blocks to "Done" with the date. Update roadmap markers.
4. Open the milestone and each block with a "Why" paragraph: what problem it solves, how it fits the project, what breaks without it. Then write the next block(s), each finishable in one sitting, each step with a why, each Unity step with the exact menu path, each block ending in a checkpoint.
5. Write each file as a skeleton handoff per `CLAUDE.md` rule 3: imports, signatures, docstrings, TODO comments with hints, no logic. Include a "Concepts you will need" list, verify in the scratchpad per rule 5, and end each block with "Check your understanding" questions.
6. No files under `sim/` or `unity/`. No em dashes.
